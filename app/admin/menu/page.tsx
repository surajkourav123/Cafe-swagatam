import { MenuClient } from './menu-client';

export const metadata = {
  title: 'Menu Manager — Swagatam Cafe Admin',
  description: 'Add, update or delete products on the digital cafe ordering menu.',
};

export default function AdminMenuPage() {
  return <MenuClient />;
}
