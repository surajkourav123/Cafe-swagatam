import { CartClient } from './cart-client';

export const metadata = {
  title: 'Cart — Swagatam Cafe',
  description: 'View your food cart and place your order online.',
};

export default function CartPage() {
  return <CartClient />;
}
